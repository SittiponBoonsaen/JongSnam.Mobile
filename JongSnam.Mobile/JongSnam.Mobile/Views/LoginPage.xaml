<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="JongSnam.Mobile.Views.LoginPage">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="สมัครสมาชิก" Command="{Binding RegisterCommand}"/>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout BackgroundColor="#f5f5f3" Padding="20">
           
            
            <Label Text="Login" FontSize="Title" HorizontalOptions="Center" />
            <Image Source="icon_logo.png"/>

            <Entry Placeholder="Email" Text="{Binding UserName}" PlaceholderColor="Gray" Keyboard="Email" MaxLength="20" />
            <Entry Placeholder="Password" IsPassword="true" Text="{Binding Password}" PlaceholderColor="Gray" MaxLength="20" />
            <Button Text="Login"
                VerticalOptions="CenterAndExpand"
                BackgroundColor="RoyalBlue"
                Command="{Binding LoginCommand}"    
                />
            <Label Text="version1.1" HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>